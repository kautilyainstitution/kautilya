export interface snapPattern {
    num: number;
    sections: string;
    count: number;
    marks:number;
}

export interface snapCutoff {
    institute:string;
    percentile: number;
    score:number;
}