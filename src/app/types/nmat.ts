export interface nmatPattern {
    section: string;
    count: number;
    timelimit:number;
    score:string
}

export interface nmatCutoff {
    locations:string;
    percentile: string;
    score:string;
}